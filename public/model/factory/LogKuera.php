<?php
class LogKuera {
	public function LogKuera() {
	}
	public static function log($operacao) {
	/*	$link = mysql_connect ( DBHOST, DBUSERNAME, DBPASSWORD );
		try {
			if (! $link) {
				throw new Exception ( 'Falha na conexao com servidor MySQL: ' . mysql_error () );
			}
			$con_db = @mysql_select_db ( DBNAME, $link );
			if (! $con_db) {
				throw new Exception ( 'Falha na selecao do banco engecar_site: ' . mysql_error () );
			}
		} catch ( Exception $e ) {
			throw new Exception ( $e->getMessage () );
		}
		$query = "insert into public_logkuera (operador,dtoperacao,operacao)values('desenv',now(),'" . $operacao . "')";
		$result = mysql_query ( $query, $link ) or die ( 'Query failed: ' . mysql_error () );*/
	}
}
?>

